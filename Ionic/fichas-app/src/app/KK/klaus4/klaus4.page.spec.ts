import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Klaus4Page } from './klaus4.page';

describe('Klaus4Page', () => {
  let component: Klaus4Page;
  let fixture: ComponentFixture<Klaus4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Klaus4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
