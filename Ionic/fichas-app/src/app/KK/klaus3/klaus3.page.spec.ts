import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Klaus3Page } from './klaus3.page';

describe('Klaus3Page', () => {
  let component: Klaus3Page;
  let fixture: ComponentFixture<Klaus3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Klaus3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
