import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Klaus6Page } from './klaus6.page';

describe('Klaus6Page', () => {
  let component: Klaus6Page;
  let fixture: ComponentFixture<Klaus6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Klaus6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
