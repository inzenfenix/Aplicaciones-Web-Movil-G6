import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Klaus2Page } from './klaus2.page';

describe('Klaus2Page', () => {
  let component: Klaus2Page;
  let fixture: ComponentFixture<Klaus2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Klaus2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
