import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Klaus5Page } from './klaus5.page';

describe('Klaus5Page', () => {
  let component: Klaus5Page;
  let fixture: ComponentFixture<Klaus5Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Klaus5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
