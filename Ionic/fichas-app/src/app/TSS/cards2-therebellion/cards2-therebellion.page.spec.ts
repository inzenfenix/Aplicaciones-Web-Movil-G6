import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Cards2TherebellionPage } from './cards2-therebellion.page';

describe('Cards2TherebellionPage', () => {
  let component: Cards2TherebellionPage;
  let fixture: ComponentFixture<Cards2TherebellionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Cards2TherebellionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
